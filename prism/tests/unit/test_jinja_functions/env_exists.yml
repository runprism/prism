profile_name:
  adapters:
    snowflake_adapter_name_here: # change this!
      type: snowflake
      user: "{{ env('SHELL') }}"
      password:
      account:
      role:
      warehouse:
      database:
      schema:
