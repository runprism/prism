profile_name:
  adapters:
    snowflake_adapter_name_here: # change this!
      type: snowflake
      user: "{{ env('THIS_SHOULD_FAIL') }}"
      password:
      account:
      role:
      warehouse:
      database:
      schema:
