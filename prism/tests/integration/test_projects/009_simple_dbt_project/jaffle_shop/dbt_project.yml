name: 'jaffle_shop'

config-version: 2
version: '0.1'

profile: 'jaffle_shop'

task-paths: ["tasks"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_tasks"
    - "logs"

require-dbt-version: [">=1.0.0", "<2.0.0"]

tasks:
  jaffle_shop:
      materialized: table
      staging:
        materialized: view
